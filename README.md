## Run batch simulations

To run many simulations with different input parameters, use
```bash
python3 run_simulatations.py --market-value $MARKET_VALUE --retirement-value $RETIREMENT_VALUE
```
this will require specifying market and retirment account values, and salary information. The result of this command will be exps.csv which contains result data from each simulation.
